TASK 052 - Default init radius 1024

Mission:
Set the default init radius to 1024 across deploy scripts, client config, and docs.

Scope:
- Update defaults in scripts/config/docs where 1000 is the default init radius.

Plan:
1) Update DEFAULT_INIT radius in deploy scripts and client config.
2) Update README/client README defaults to 1024.
3) Update any helper scripts that bake default init radius values (bench/e2e).

Acceptance criteria:
- Default init radius is 1024 in deploy_local.js and deploy_devnet.js.
- Client default VITE_INIT_RADIUS is 1024.
- Docs reference 1024 as the default init radius.
